package tt.game.m2m.model

import scalafx.scene.image.Image

abstract class SpaceShip(
                          val name: String,
                          val image: Image,
                          val size: Double
                        )
